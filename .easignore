node_modules/
.git/
.expo/
*.log
*.apk
*.aab

# Local Gradle output. Regenerated by every build and ~185 MB here, so
# uploading it only slows the archive down — EAS builds android/ from source.
android/build/
android/app/build/
android/app/.cxx/
android/.gradle/
